# == Schema Information
#
# Table name: users
#
#  id         :integer          not null, primary key
#  exuid      :string(32)       not null
#  created_at :datetime
#  updated_at :datetime
#  admin      :boolean          default(FALSE), not null
#

admin:
  exuid: 12345
  admin: true

dexter:
  exuid: 56789

inactive:
  exuid: 66666

solo:
  exuid: 98765
