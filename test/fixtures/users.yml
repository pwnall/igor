# == Schema Information
#
# Table name: users
#
#  id         :integer          not null, primary key
#  exuid      :string(32)       not null
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

robot:
  exuid: robot000exuid

admin:
  exuid: admin000exuid

admin_2:
  exuid: admin_2000exuid

dexter:
  exuid: dexter000exuid

deedee:
  exuid: deedee000exuid

mandark:
  exuid: mandark000exuid

inactive:
  exuid: inactive000exuid

solo:
  exuid: solo000exuid

main_dropout:
  exuid: main_dropout000exuid

main_staff:
  exuid: main_staff000exuid

main_staff_2:
  exuid: main_staf_2000exuid

main_grader:
  exuid: main_grader000exuid

main_grader_2:
  exuid: main_grader_2000exuid

not_main_student:
  exuid: not_main_student000exuid

not_main_dropout:
  exuid: not_main_dropout000exuid

not_main_staff:
  exuid: not_main_staf000exuid

not_main_grader:
  exuid: not_main_grader000exuid
