# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  activerecord:
    attributes:
      deadline_extension:
        due_at: "Extended deadline"
      assignment:
        published_at: "Release date"
    errors:
      models:
        db_file:
          attributes:
            f:
              spoofed_media_type: "Detected MIME type does not match browser-set type."
        deadline_extension:
          attributes:
            due_at:
              after: "must occur after the original deadline."
        assignment:
          attributes:
            published_at:
              before: "must occur before the base deadline."
              invalid_datetime: "must be a valid date (not undecided)."
