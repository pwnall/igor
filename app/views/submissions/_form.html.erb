<%= form_for submission, :html => {:multipart => true} do |f| %>
  <% f.object.build_db_file unless f.object.db_file %>
  <%= f.fields_for :db_file do |fu| %>
    <%= fu.file_field :f %>
  <% end %>
  <%= f.submit "Upload" %>
  <%= f.hidden_field :deliverable_id %>
<% end %>
