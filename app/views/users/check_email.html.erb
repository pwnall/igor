<% if @email.length < 5 %>
	<span class="error-text">too short</span>	
<% else %>
	<% if @user %>
		<span class="error-text">already registered</span>
	<% else %>
	  <%= ok_image_tag %>
	<% end %>
<% end %>
