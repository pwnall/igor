if @success
  add_site_status page, (@new_record ? 'Deliverable validation created' : 'Deliverable validation updated'), true
else
  add_site_status page, (@new_record ? 'Failed to create deliverable' : 'Failed to update deliverable'), false
end
page.replace "submission_checker_#{@deliverable.id}", :partial => 'show_for_deliverable', :locals => {:deliverable => @deliverable} 
