<%= form_for(@profile_photo) do |f| %>
  <% if @profile_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_photo.errors.count, "error") %> prohibited this profile_photo from being saved:</h2>

      <ul>
      <% @profile_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pic %><br />
    <%= f.text_field :pic %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
