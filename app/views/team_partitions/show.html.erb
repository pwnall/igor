<h2>Team partition <%= @team_partition.name %></h2>

<dl class="groups">
	<% @team_partition.teams.each do |team| %>
	<%= render team %>
	<% end %>
</dl>

<h3>Add Team</h3>
<%= render 'teams/form' %>
