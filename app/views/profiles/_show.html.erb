<div class="profile-container">
<h3>
  Profile
  <% if profile.editable_by_user?(current_user) %>
  <%= link_to edit_profile_path(profile), :class => 'button' do %>
    <%= image_tag('pencil_16.png') %> Edit
  <% end %>
  <% end %>
  <span id="profile-ajax-indicator"></span>
</h3>
<%= render profile %>
</div>
