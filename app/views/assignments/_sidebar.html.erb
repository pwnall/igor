<div id="assignments-sidebar">
  <p class="actions">
    <%= link_to new_assignment_path(course_id: current_course),
          class: 'button emphasized' do %>
      <%= create_icon_tag %> New Assignment
    <% end %>
  </p>

  <%= render 'assignments/sidebar_list',
             assignments: current_course.assignments_for(current_user) %>
</div>
